import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;

import static java.util.stream.Collectors.counting;
import static java.util.stream.Collectors.groupingBy;

public class HelloWorld {

//
//
//        public static void main(String[] args) {
//            String s1 = "This is my first program first program";
//            String[] list = s1.split(" ");
//
//            List<String> strList = Arrays.asList(list);
//            strList.stream().collect(groupingBy(s -> s,counting())).entrySet().stream().forEach(x->System.out.println(x.getKey()+"-"+x.getValue()));
//                  //  .collect(groupingBy(x ,counting())).entrySet().forEach(x-> System.out.printLn(x.getKey()+ "-"+x.getValue()));
//
//
//
//
//            System.out.println("Try programiz.pro");
//        }
    }

